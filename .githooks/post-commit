#!/bin/sh

ERP_DIR="../erpclinicatea"

if [ -d "$ERP_DIR/.git" ]; then
  echo "[TEAjudando] Sincronizando e publicando no ERP Clinica TEA..."
  (cd "$ERP_DIR" && pnpm run comunicatea:publish)
else
  echo "[TEAjudando] ERP nao encontrado em $ERP_DIR. Sincronizacao ignorada."
fi
